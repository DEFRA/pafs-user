<p>Hello <%= @resource.full_name %></p>

<p>We have received a request to reset the password for <%= @resource.email %>.</p>

<p>Use the link below, or copy and paste it into your browser, to reset your password.</p>
<p><%= link_to nil, edit_password_url(@resource, reset_password_token: @token) %></p>

<p>This link will expire after 6 hours.</p>
<%= render partial: "shared/email_support" %>
