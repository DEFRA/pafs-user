<h2 class="heading-large">Sign in to your account</h2>
<p class="lede">I love cheese, especially goat swiss. Stinking bishop caerphilly say cheese macaroni cheese feta halloumi fromage frais cut the cheese. Danish fontina parmesan lancashire paneer port-salut cut the cheese red leicester blue castello. Cottage cheese st. agur blue cheese.</p>
<%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
  <div class="form-group">
    <%= f.label :email, class: 'form-label' %>
    <%= f.email_field :email, autofocus: true, class: 'form-control' %>
  </div>

  <div class="form-group">
    <%= f.label :password, class: 'form-label' %>
    <%= f.password_field :password, autocomplete: "off", class: 'form-control' %>
  </div>

  <% if devise_mapping.rememberable? -%>
    <div class="form-group">
      <%= f.label :remember_me, class: 'form-checkbox' do -%>
        <%= f.check_box :remember_me %> Remember me
      <% end -%>
    </div>
  <% end -%>

  <div class="form-group">
    <%= f.submit "Sign in", class: 'button' %>
  </div>
<% end %>

<%= render "devise/shared/links" %>
